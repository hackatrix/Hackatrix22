<<<<<<< HEAD
<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- Estilos de Flat Buttons Login -->
    <style name="Holo_Flat_Button_Facebook" parent="@android:style/Widget.Button">
        <item name="android:height">48dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:drawableLeft">@drawable/ic_btn_facebook</item>
        <item name="android:background">@drawable/selector_btn_fb</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- imageButton -->
    <style name="Holo_Flat_Button_Verde" parent="@android:style/Widget.ImageButton">
        <item name="android:height">48dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:background">@drawable/selector_tab</item>
    </style>
    
    <!-- imageButton send -->
    <style name="Holo_Flat_Button_Naranja" parent="@android:style/Widget.ImageButton">
        <item name="android:height">48dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:background">@drawable/selector_btn_send</item>
    </style>
    
    <!-- button invite -->
     <!-- Estilos de Flat Buttons Login -->
    <style name="Holo_Flat_Button_Invite" parent="@android:style/Widget.Button">
        <item name="android:height">48dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:drawableLeft">@drawable/ic_action_send</item>
        <item name="android:background">@drawable/selector_btn_invite</item>
    </style>

</resources>
=======
<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

</resources>
>>>>>>> 5a4d2cc97694610ba33a67abf53c8df40f671883
